# Data Directory

Expected local dataset file:
- creditcard.csv

This file is not tracked in git due to repository file-size constraints.
Use the repository helper script to fetch it:

```bash
bash scripts/fetch_creditcard_dataset.sh
```
